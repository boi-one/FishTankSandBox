using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class GoToPond : MonoBehaviour
{
    // Start is called before the first frame update
    public void Pond()
    {
        SceneManager.LoadScene(1);
    }
}
